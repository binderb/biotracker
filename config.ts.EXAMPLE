const isServer = typeof window === 'undefined';

const config = {
  webTitle: 'Example Title',
  development: {
    uri: isServer ? 'http://localhost:3000/api/graphql' : '/api/graphql',
  },
  production: {
    uri: isServer ? 'https://example.com/api/graphql' : '/api/graphql',
  },

};

export default config;